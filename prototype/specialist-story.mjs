// Shared reading contract extracted from the approved D host; no business actions.
export function specialistStoryContract(beat){
 const titles={D1:['Parallel work, one shared case','并行工作，同一案件'],D2:['Conflicts starts early and stays bounded','利益冲突尽早启动且边界清晰'],D3:['Credit produces a versioned condition','信贷生成版本化条件'],D4:['Legal consumes a named Credit input','法务使用明确版本的信贷输入'],D5:['Readiness keeps every unresolved condition visible','就绪状态保留每项未决条件']};
 const work={D1:['Conflicts, Credit and Legal advance on their own prerequisites.','利益冲突、信贷与法务依据各自前置条件推进。'],D2:['Search is complete; a specialist reviews the open finding.','搜索已完成；专业人员复核未决发现。'],D3:['Track CC-01 and pass a revision-addressed AI-01 to Legal.','跟踪 CC-01，并向法务传递带版本的 AI-01。'],D4:['Keep AGR-01 revision 03 frozen while checking whether its Credit input remains current.','冻结查看 AGR-01 版本 03，同时检查其信贷输入是否仍为当前版本。'],D5:['Inspect every unresolved condition and the authority boundary.','检查每项未决条件及权限边界。']};
 const wait=beat==='D2'?['Configured Conflicts review and scoped hold resolution.','等待已配置的利益冲突复核及限定 Hold 解决。']:beat==='D3'?['Documentation incorporation before execution.','等待签署前纳入文件条件。']:beat==='D4'?['Validated signatory authority and execution; current Credit input if revised.','等待经验证的签署权限与签署；如信贷已修订，还需当前输入。']:['Conflicts review, Legal execution, QA and unresolved C conditions.','等待利益冲突复核、法务签署、QA 及 C 阶段未决条件。'];
 const output=beat==='D3'?['A versioned Credit condition and agreement input, not case clearance.','形成版本化信贷条件与协议输入，不代表案件准入。']:beat==='D4'?['An internally approved agreement remains pending execution.','内部已批准的协议仍等待签署。']:['A scoped contribution to a still Not Ready case.','对仍为尚未就绪案件的一项限定贡献。'];
 return {title:titles[beat]||titles.D1,work:work[beat]||work.D1,wait,output,current:['Current: handoffs are interpreted across separate records and version currency is easy to miss.','Current：交接分散在多个记录中，版本是否最新容易被忽略。']};
}
