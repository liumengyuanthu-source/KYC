# Product Capability Map

Status: D2 Batch F requirement hypotheses, not bank-approved requirements.

The canonical catalogues are `capability_map.csv`, `requirement_backlog.csv`, `requirement_traceability.csv` and `acceptance_criteria.csv`. Ten families use CAP-001–010 and CTT-REQ-001–010, allocated once after the F namespace check. Existing priority/score columns are deliberately empty; no MVP ranking is made.

The Studio Product Definition and F JSON exports are derived by `04_operating_model/batch-f/build-consolidation.mjs`. They cannot write case state. Each workshop-facing requirement includes scenes, process references, source/assumption, a human boundary and observable Given/When/Then acceptance. Validation results are in `00_governance/batch-f/red-team-master.json`; inherited evidence and newly executed checks are separately dated.
