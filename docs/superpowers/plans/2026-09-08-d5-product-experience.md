# D5 Product Experience implementation plan

Goal: implement D5U-T00–T06, a reviewable Person T pilot, not the complete fifteen-scene rollout.
Spec: `00_sources/d5/Clear_to_Trade_D5_Product_Experience_Review_v0.2.md` and its bindings JSON. User explicitly requests implementation and delegates UX/UI design.
Architecture: additive native ES modules, pure role-safe projections over existing engines; one current operational session, separate navigation/preferences/history/author demo. No new operational truth store.
Stack: existing local HTML/CSS/ES modules, Node test runner, installed Playwright/Chrome, existing Archify static outputs. No dependency migration.

## Global Constraints

- Preserve stable IDs, current Entity A / Entity B / Person T case, source statuses, and all existing D2–D4 business rules. Person T authority remains unknown.
- Current repository visual tokens and user changes are authoritative. No new hex palette, violet or mint, D4 score/action IDs as prominent Product content, or whole-site redesign.
- Progress, navigation, playback, layout, history and media cannot mutate operational Case, grants, decisions or readiness. Required work and permission/error explanations cannot be hidden by preferences.
- Author checkpoints and synthetic time examples are not live Product state. Waiting is not blocked; local completion is not Case clearance; estimates are not bank SLA or client commitments.
- Role projection is local demo behavior, not authentication. Filter before DOM, counts, search, tooltips, exports. History requires current access, not past participation alone.
- Keep all return paths, latest language and current saved Case state; Save / Discard / Stay protects actual business edits. Print complete selected records, not just viewport.
- No bank policy invention, production integration, model runtime, remote publishing, push, commit, reset, worktree migration, or unrelated-file changes. Stop at D5U-T06 review gate; do not execute T07 / D6 / D7.

## Preparation (controller, D5U-T00)

Archive the supplied four-file source pack in `00_sources/d5/`, retain its original verification as document-only. Snapshot current source into `00_governance/d5/baseline/` before edits. Record git head, actual color/font paths, run baseline unit tests and capture Product screenshot. Build file/delta and source-qualified action/work/source crosswalk. Existing D4 source hash must match metadata; do not mint replacement business IDs.

### Task 1: Person T Product workspace and read-only supporting projections

Owner: one implementation agent. Controller owns source/governance, browser QA, print proofs and final reports independently. Do not edit `prototype/qa/`, `00_sources/`, `00_governance/`, `07_output/`, or diagrams; send controller selectors/APIs when ready. Implementation owns new `prototype/product-experience/` focused modules, new `prototype/tests/product-experience*.test.mjs`, optional scoped CSS file, minimal `app.mjs` / `navigation.mjs` / `index.html` / `screening-ui.mjs` / collaboration UI hooks. Preserve five engines unless an actual unavoidable interface problem is reported first.

Read the complete D5 spec and inspect bindings before implementation, including sections 18–27 and original 1–17. Implement T01–T05 requirements and the original Person T P1–P8 journey by reusing current C/B commands and guards. This plan is an implementation argument, not a replacement for the source.

1. Write behavior tests first and record RED before production code. Tests cover role-safe projections, source/version binding, progress state separate from viewing state, semantic adapter, time intervals, unknown/conditional estimates, re-review episodes, prefs, current-access history and forbidden writes. Use literal independent assertions, not mirrored builders. Maintain focused responsibilities and files under 1000 lines.
2. Add a single pure shared progress/status projection from actual WorkItems/events/conditions, with five case stages and parallel specialist lines plus six MATCH display groups. Preserve unknown manifest completeness. Show owner separately from next actor/waiting party; timestamps are null unless actual event evidence exists. Selecting a step is navigation only, including future steps. Expose the API clearly for controller QA. Shared semantic adapter separates work/health/next/freshness and implements all ten semantic statuses with icons + readable text, including unknown and not-applicable basis.
3. Add pure temporal projection preserving timestamps/timezone, non-overlapping residence intervals, frozen historical as-of and re-review episode history. Actual session facts must not be manufactured from page opening. Include the exact `D5U-TIME-DEMO-01` in a visibly separate illustrative panel: Sydney 09:00 queue, 09:30 started, 10:00 waiting, 14:00 as-of, elapsed 5h / queue .5h / in-progress residence .5h / wait 4h, human effort unknown. Its 1–2 business days estimate is conditional, internal, synthetic, not client-shareable. No calendar finish, sum of parallel ETA, or invented SLA. Estimate revisions retain basis/version/reasons.
4. Build task-first Product UI: compact case/task/role/return, current issue and next action, evidence comparison occupying roughly two thirds, stable human form/action third. Reuse actual C finding/version/evidence/disposition eligibility. No fallback to older Entity A finding, no new authority or successful exclusion fixture. Received is not assessed; unknown DOB is not different. Source detail uses restrained text/disclosure. Actual save/request/referral result stays in place and other Case conditions remain visible. Before a valid C session exists show explicit open-example/prepare-existing-query path, not silent fixture reset.
5. Four fixed core modules remain visible and immutable. Optional timeline/dependencies/nonmandatory evidence/contacts/history/notes catalog maps semantic intent to existing data; RM email helper is nice-to-have, not a blocker. Implement allowed add/remove/move/collapse/preview/save/cancel/reset with role defaults. Preferences key includes persona/user fixture, role, workspace type and schema version; preference stores IDs/order/collapse only. Re-evaluate access at render and filter catalog before DOM. Same role, different persona does not share prefs. Core pending/errors/required form cannot be removed. Layout cancel/reset cannot discard business draft, locale, history or Case.
6. Add lightweight Cases (Active, My completed work, Archived) using shared pre-filtered current-access projection. History snapshots are read-only, frozen/versioned, no loading over live state, reopening or copying clearance. Preserve filters/scroll/current edits via guard and return. Provide three minimal read-only history sample mappings where supported by existing fixtures (historically completed with scope; ended non-clearance; my completed task while Case active). If source fixtures cannot substantiate a case outcome, report the gap and use a plainly labeled unpopulated sample slot; never fabricate approval of the current hero case. Client only sees own request history under current valid grant/persistent authority, otherwise an RM contact safe state, no global internal counts.
7. Mandatory RM-safe progress and Email-default relationship metadata, retaining secure contribution path. Optional email templates/draft/copy may be deferred and reported NOT_RUN; no inert copy/send buttons. If implemented, input only safe projection, preserve edited draft on refresh, estimates require approved client-shareable current basis, unknown recipients remain placeholders, copy never sends or writes Activity as sent. No mailbox integration.
8. Integrate all zh-CN/en-AU/en-US labels/errors/empty states/times/layout/history/print, retaining original user rationale. Keep exact Studio Current/Target / OM-action return tokens, latest role/language, local saved state, narrow layouts, keyboard/touch/reduced-motion. Print full selected scope and all required content even if optional modules hidden, with version/as-of/source/estimate qualifiers; filter RM/Client before HTML generation. Reuse existing Archify graph static manifest only for appropriate dependencies, with readable fallback. No graph navigation writes.
9. Run focused tests throughout, then full unit suite once. Self-review changed code. Report exact APIs, DOM selectors, navigation and preference storage keys, fixture limitations, tests with RED/GREEN commands/output and files changed in the task report. Do not claim all D5 acceptance passes from units; controller handles actual host evidence.

No subagents, no commits, no bank operations. Report DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Ask controller for concrete missing interface context rather than silently dropping requirements.

## Verification and handoff (controller, D5U-T06)

Independently exercise actual host pilot, source/version guards, request/write boundaries, custom layouts/persona changes, history access and frozen clocks, Save/Discard/Stay, Studio/OM return, latest language, missing static media, keyboard/touch/reduced motion, responsive screenshots and full selected-record PDFs. Run prior A/B/C/D/D3A/D4 regressions in a separate D5 evidence directory, preserving historical receipts. Map all 88 acceptance IDs to actual passed/failed/not-run evidence; missing QA/Lab/runtime or optional email are explicit NOT_RUN, not all-green summary.

Provide an exact baseline-to-current diff package (HEAD alone misses untracked prototype), fresh task review, fixes and scoped re-review, then whole-increment final review. Retain no-commit ledger/review evidence. Deliver local mainline preview, screenshots, schema/field/dependency delta, unresolved questions and review gate. Do not automatically expand remaining scenarios.
