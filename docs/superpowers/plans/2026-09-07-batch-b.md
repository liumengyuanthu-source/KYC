# D2 Batch B approved collaboration increment
## Global constraints
Only B-CH00–05 in SRC-015 Approved Addendum v1.0. Same DEMO-CTT-001, Entity A/B/Person T and existing IDs. Request-first pages must be backed by requirements/purposes. Preserve Batch A unknowns. No remote services, no commit/push, no site rebuild. Black/blue/white. local_simulation only. User asked continuation in the existing dirty workspace; keep it in place and preserve unrelated work.
## Task 1: Business model and tests
Read /Users/christinaliu/Downloads/Clear_to_Trade_D2_Batch_B_Client_Collaboration_Approved_Addendum_v1.0.md fully, existing prototype/batch-a-engine.mjs, case-engine.mjs, and batch-a-story-snapshots.json SNAP-A3.
Own ONLY prototype/collaboration-engine.mjs and prototype/tests/collaboration.test.mjs plus this task's report. No subagents. No commits. Main agent handles UI, schema artifacts and diagrams.
Implement immutable actions with same root Shared Case Spine; extend existing InformationRequest authority_gap record (reuse id) into one request with two separately assigned items. Add requestItems, businessContacts, interactionRecords, requestRecipients, requestAccessDecisions, notifications, portalInvitations, accessGrants, submissions, channelPolicies, contributorSessions arrays as needed, NOT a parallel independent case. Authority requirement may reuse existing purpose-specific req if found otherwise add stable requirement/authority-coordinate and requirement/ownership-control IDs; aliases ITEM-AUTHORITY/ITEM-OWNERSHIP do not replace physical IDs. New minimal second contributor is fixture only.
Exports:
- createBatchB(input) refuses unless Batch A working_scope_recorded and A identity resolved; never resets case or unknowns; idempotent if already extended.
- COLLAB_IDS constants with request,authorityItem,ownershipItem,personT,secondContributor,recipientT,recipientOther,authorityRequirement,ownershipRequirement.
- collaborationAction(input,a) => immutable next entire shared case; throws Error on rejected action.
- collaborationProjection(input,{audience,userId,sessionId,now}) => a safe allowlisted projection. audience ops/rm/client. Never include restricted reasons in RM/client payload. client with missing/invalid auth or grant returns allowed:false,reason, no items/artifacts. Repeat checks for every read and write; expired/revoked/mismatched principal grant fails closed.
Actions contract a={type,role,expectedRevision,expectedRequestRevision,expectedItemRevision,itemId,recipientId,userId,sessionId,key,text,rationale,channel,intakeStatus,now,reference}. Actor role ROLE-KYCOPS, ROLE-RM, ROLE-CLIENT. Default deterministic mock config local_simulation official_site_reference; no raw tokens, no real emails. Time checks injected now optional actual new Date ISO.
Types: save_request, review_request, dispatch, record_note, refer_question, start_session, save_response, submit_response, staff_submit, release_artifact, link_evidence, record_assessment, request_more, refer_review, revoke_grant, replace_recipient.
Review request requires mock contact/disclosure/access review config; creates version-bound review/access decisions and limited per-recipient grants, no notification. dispatch uses only configured channel (default official_site_reference), preserves sent request/item snapshot and creates separate dispatch/delivery unknown record, duplicate sends idempotent; wrong channels rejected. start_session valid reference + fixed synthetic identity authenticates but requires grant separately for projection; records contributor_session_started. No reference-only authentication/access.
Request items have alias,subject_id,principal_entity_id,purpose_code,requirement_id,revision,response_status,client_reason,restricted_reason,response_draft and projection only grant-assigned items. client projection common fields: allowed,case_id,case_revision,request_id,request_revision,entity_label,items,submissions; safe items include id,alias,purpose_code,client_reason,revision,response_status,response_draft; safe receipts no other submitter info. RM returns client-safe all request item progress + reviewed interaction notes, not artifact contents. Ops projection can include request/items/contacts/notifications/submissions/assessments and internal reasons.
save_response/submit_response guard request+item+scope+session+grant. Submission synthetic content preset (no real upload), draft text required; append Evidence artifact (alias EV-A05 for first coordination evidence, later unique IDs), preserve source/submitted_by/on_behalf_of/staff uploader and duplicate candidates. Submit item->under_review only, other item open. Initial intake received or quarantined, release_artifact named mock scenario released/rejected/quarantined; no real scan claim. link_evidence explicit purpose association. record_assessment only released linked artifact, current scope/item/evidence revisions, ROLE-KYCOPS permission and rationale; append versioned EvidenceUseAssessment with existing 4-value sufficiency, coordination sufficient demo only, old insufficient preserved; only linked req/task updates, authority coordinate established_for_demo or existing compatible explicit status; signing/trade unchanged, overall Not ready. Unknown dependency currency becomes needs_review.
Every new behavior/object retains purpose_class, implementation_fidelity, business_validation_status, source_refs ['SRC-015']; provenance synthetic. Existing old objects need not bulk rewrite. Scope change revokes current request review/grant or projection checks scope mismatch; any assessment bound to older scope not projected as current.
Test-first: export stubs then first real failing assertions, retain output report. Implement CH-01..20,26..28 model coverage; CH-06 email adapter disabled not-run; CH-21/22/23/24/25 UI/main tests later. Ensure all existing tests pass.
Send main agent early concise API / collection shape confirmation before long implementation. Write detailed report 00_governance/batch-b/engine-report.md with files/tests/concerns.
## Task 2: Existing UI integration
Controller owns collaboration-ui.mjs, collaboration.css and app integration; no new top-level entry. Ops Requirements/Evidence/Requests/Contacts/Tasks/Activity; RM Client requests; client Your tasks/Documents/Submitted/Help. Workshop outside product holds role/mock identity/auth and return. Safe projection before DOM; client print independently scoped; Save/Discard/Stay keeps failed inputs.
## Task 3: Diagram and artifacts
Controller reads local Archify, builds a small Request→Recipient/Submission→purpose assessment→case workflow; static fallback. Schema/field/source mapping extends Batch A; no bare DEP renumber. Record missing research source rather than invent.
## Task 4: Review and regression
Independent review of model and integration. Execute CH and prior tests, isolated browser, screenshots, current/target/locale/dirty/client print, no remote deployment. Deliver change report with honest passed/failed/not-run and next-group interface.

