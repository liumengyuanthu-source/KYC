# D3A PC-01 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** Implement the approved scenario-first process comparison and PC-01, stopping for T04 human calibration.
**Architecture:** Add a read-only author registry over D3's existing index. Use one inline modal panel system and existing product actions/navigation. Archify produces separate Current/To-be taxonomy projections; HTML mapping lists link selection without pretending mapping is execution.
**Tech Stack:** Existing native ESM, HTML/CSS, Node test runner, approved local Playwright/Chrome, Archify2.17.
**Spec:** `/Users/christinaliu/Downloads/Clear_to_Trade_Discussion_3A_Process_Reconstruction_Studio_UX_Final_v1.0.md`

## Global Constraints

- Preserve Entity A / FX forward, reported parent Entity B, Person T employed by B; coordination is not signing authority.
- Existing source occurrence IDs, D3 change IDs, business writers, fixtures, schema and permissions remain unchanged.
- Read-only author content and navigation never restore a Case snapshot, mutate event logs, approve or publish.
- Retain two top-level entries and S1–S5. No new portal, sidebar, nested Scenario modal, policy or business branch.
- Three local values `current/target/compare`; display Target as To-be in the comparison. Do not duplicate comparison switches.
- Black/blue/white glass; no violet or mint. IDs secondary, body readable; no teaching subtitle on normal product pages.
- Three locales `zh-CN/en-AU/en-US`; inherited source verification is not fresh research. Hypotheses remain labelled.
- Every change has exactly one primary branch; proposed addition has no fabricated direct Current predecessor.
- Keep dirty checkout in place, no commits, push, cleanup or external publication. Preserve historical QA.
- T04 is an explicit human gate: no PC-02/03/04 or remaining full graph/UI expansion before calibration.

### Task 1: Author registry and PC-01 content (T01/T02 data)

**Files:** Create `prototype/reconstruction/registry.mjs`, `prototype/reconstruction/pc01.mjs`, `prototype/reconstruction/copy.mjs`, `prototype/tests/reconstruction-registry.test.mjs`. No host edits.
**Consumes:** `transformationIndex`, `transformationSources`, `transformationReferences`, `transformationIssues` from `prototype/transformations/content.mjs` and immutable original D3 JSON.
**Produces:** `branches` array; `mappings` array; `nodes` array; `pc01ChangeIds` array; `branchForScene(id)`; `comparisonFor(sceneId,locale)` returning null outside PC-01's four scenes. Each projected comparison has branch, sceneId, checkpoint, topics, nodes, mappings, traces, references and revision. Report exact shape for UI consumer.

- [ ] Write real failing tests before implementing, e.g. `assert.equal(branches.length,12); assert.equal(new Set(mappings.flatMap(m=>m.change_refs)).size,94)` and exact prescribed scene/branch assignments; corrupt refs and duplicate assignments must be rejected by exported validation.
- [ ] Run `node --test prototype/tests/reconstruction-registry.test.mjs`; record RED then GREEN.
- [ ] Map BR-01 scope;02 entity;03 requirements;04 source+validate;05 gap;06 population;07 match;08 EDD;09 conflicts;10 credit;11 legal;12 QA/readiness/publish. S1–S5 aliases from current navigation. BR12 has four independent subroots QA/remediation/readiness/authorisation-publication, taxonomy only.
- [ ] Retain all94 original types, flags, actors and exact source references; source ambiguities remain unresolved. Proposed addition before refs empty, context refs separate. Other94 registry mappings may be provisional author mapping, never bank validation.
- [ ] PC-01 implements exactly the seven specified priority mappings: REQUIREMENTS01/03, SOURCE03, GAP01/03/05, VALIDATE04. Four scenario entries default to their applicable mapping; VALIDATE04 is explicit walkthrough default. Provide meaningful short bilingual node labels and full localized explanations, common checkpoint and input manifest. Do not invent paid-source access, fixtures or evidence. Existing fixture ref may be a documented design reference, never loaded by this content API.
- [ ] Use source activities as shared Current nodes where appropriate; multiple To-be subnodes only for real distinct design work. Preserve group membership instead of cartesian edges. Proposal Current is empty, readable context only. Mapping classification/status is not business state.
- [ ] Add explicit linked Pain(hypothesis) → Opportunity → Solution/Capability/Requirement/Acceptance records for PC-01, not one fabricated pain per node. Preserve null pain for controls. Localize displayed reference observations/adaptation/limits; keep raw source separately. Fields read-only; do not manufacture operational EvidenceUseRef/RequestItem.
- [ ] Test frozen data, complete and unique mapping, missing/source-format refs, proposal cardinality, shared input, localization, source flags and unknown branches. Run full existing suite once. Self-review, report exact API, TDD evidence and files; no commit.

### Task 2: Reusable PC-01 host comparison (T03)

**Files:** Create `prototype/reconstruction-ui.mjs`, `prototype/reconstruction.css`, `prototype/tests/reconstruction-ui.test.mjs`; modify `prototype/app.mjs`, `prototype/navigation.mjs`, `prototype/index.html` only as needed. Additional small helper permitted if host event/history coupling demands it, report before changing other files.
**Consumes:** Task1 API and diagram manifest at `prototype/reconstruction/diagram-manifest.mjs` (controller supplies actual paths). Existing `sceneStep`, product writers and return state stay canonical.
**Produces:** Same-modal Scenario / Process comparison / References panels; local mode and semantic branch/mapping/node state; targeted product/back; read-only branch print renderer.

- [ ] Write failing tests for scenario-first panel, three-way local mode, no business payload in view state, proposed empty predecessor, exact group selection, hypothesis in print, unknown node revision fallback, not-included target. Run focused RED.
- [ ] Read current host/navigation/render/print/event handlers. For four PC-01 scenes replace stacked comparison-first body with business summary and explicit Compare process CTA, preserving direct Target product where legitimate. Other scenes retain existing behavior.
- [ ] One comparison component: compact branch selector for03/04/05, three-way Current/To-be/Compare, shared checkpoint, 4–8 topic titles or PC-01 subset, Archify static sides plus accessible mapping list. Select group highlights all members, no implied all-to-all links. Nodes select only; expand and source controls separate. Inline detail below, not sidebar; explicit details scroll only. Provide filter+clear empty state, previous/next mapping, overview/read/focus and reset view without changing Case.
- [ ] Add a compact secondary Process branches index inside Studio (not a third top-level page). All12 author branches readable; PC-01 entries enter the same comparison component, others explicitly await T04 calibration and link existing scene context only. For BR04's two scenes, do not silently change the user's case or choose an unrelated scene. Stage semantic IDs remain unchanged; any existing label difference from3A's table is recorded, not treated as a new stage.
- [ ] References are the same modal panel, scoped to selected mapping, four labelled sections, raw sources in disclosure. Pause any existing presentation before reference/product navigation; no automatic playback resume. Keep IDs small. No fabricated play control.
- [ ] Navigation stores semantic view context only. Meaningful panel/product navigation pushes history; selection/pan/zoom do not. Reference back, branch back, scenario back, outer close/Esc and browser back/forward restore proper layer and focus. Latest locale wins. Removed mapping returns branch with explicit old-ref/revision notice, never unrelated node. Existing saved business state wins over return tokens. Existing Save/Discard/Stay failure must remain in place with input/error.
- [ ] Product entry target exists and context is valid; otherwise explicit not-included. Respect actual operational role/projection; no restricted case content added to Studio/DOM/print via client or RM view. Use conservative authored-only projection where no field permission is established; do not relabel role switching as authorization.
- [ ] Add branch print projection for selected PC-01 branch, all content outside viewport and collapsed details, numbered mappings, source footnotes, unknowns and hypothesis labels. A4 landscape/multi-page, repeat contextual header. HTML print must not load hidden business data. Use actual static diagram paths; missing assets leave complete HTML lists.
- [ ] Run focused GREEN and full suite; preserve existing data-action contracts except intentionally superseded D3-pilot-only layout tests (document update). Report TDD, files, exact host selectors, preview path and any unresolved constraints. No commit.

### Task 3: Controller diagrams, actual-host evidence and review (T02 graphs/T06 pilot coverage)

**Files:** `prototype/reconstruction/diagrams/`, diagram manifest; `prototype/qa/d3a/`, `00_governance/d3a/`, `07_output/2026-09-07-d3a-pc01-review.md`.
- [ ] Read Archify schema/common/example then author taxonomy candidate, validate/deliver six PC-01 branch-side views per authored locale; use English asset for en-US if copy identical. HTML mapping list is grouping, no workflow playback edges.
- [ ] Validate actual sources and build one file/delta/source-qualified crosswalk at T00; record E/F source availability separately from implementation.
- [ ] Execute actual local browser UX01–30 relevant paths, mark each passed/failed/not-run with concrete reason and evidence. Lab/other chains not invented to make tests pass. Run A–D regression and available E/F checks without claiming missing implementation passed.
- [ ] Inspect screenshots at desktop/mobile and static diagram sizes; verify selected branch print full content and role projection, no event mutations. Record genuine failures and repair within scope.
- [ ] Per-task review after Tasks1/2 with exact pre-edit diffs, then independent final review. Preserve review records because no commit history is created.
- [ ] Stop at T04 and hand off PC-01 with preview, diagrams, registry, screenshots, test receipts, unresolved issues. T05 and post-expansion T06 remain not-run awaiting calibration.
