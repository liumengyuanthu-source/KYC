# D3 transformation pilot and human-readable hierarchy

> For agentic workers: use superpowers:subagent-driven-development task-by-task; controller owns audit, source reconciliation and verification.

**Goal:** Import the D3 traceability layer and deliver SCN-GAP / SCN-MATCH same-checkpoint comparisons for human review, while reducing technical-label prominence across the active HTML experience.
**Architecture:** Native read-only content adapter attached to existing scenario content. No business engine, authority, state enum or fixture migration. Existing app and Archify assets remain the mainline.
**Tech Stack:** Native ESM/HTML/CSS, Node tests, installed Archify; browser capture according to user-approved tooling.
**Spec:** /private/tmp/ctt-d3-review.7svPlw/Clear_to_Trade_D3_Current_Target_Detailed_Transformation_v0.2.md and companion JSON; original ZIP in Downloads.

## Global Constraints

- Same DEMO-CTT-001, Entity A / FX forward, B reported parent, Person T coordination only. No E/F implementation inferred from this request.
- D3 §9.5: first SCN-GAP and SCN-MATCH, human review before expanding the other13 scenario UIs. Import all15/94 mappings without calling them fully implemented.
- Preserve stable scenario/process/source IDs and source-qualified occurrences; no business state writes by compare, locale, focus, Play, Next, printing or references.
- Target is a detailed review proposal, not confirmed bank policy. Applicability remains existing three-value model; proposed changes stay in reconciliation report.
- Only two top-level experiences. References secondary; no new portal, real API, remote publication or push.
- Preserve current black/blue/white warm glass; no violet/mint, new visual theme or true optical-refraction claim.
- Business heading/action/status first. Technical IDs/source/code/version stay available in smaller readable metadata or disclosure, not prominent headings. Critical errors, missing permission, stale-input warnings and synthetic status remain visible.
- Current→Target Product→Return restores Current and latest locale without rolling back saved operational state. Draft guards unchanged.
- Preserve dirty checkout and all old QA evidence; no commits or cleanup. New source copies/evidence live under d3 directories.

## Task 1: Read-only traceability adapter and pilot localisation

**Files:** Create `03_personas_journey/d3/build-content.mjs`, `prototype/transformations/data.mjs` (generated), `prototype/transformations/content.mjs`, `prototype/transformations/pilot-en.mjs`, `prototype/tests/transformations.test.mjs`; input `00_sources/d3/` raw pack; report task-1-report.md.

**Interfaces:** Export `transformationFor(scenarioId,locale='en-AU')`, `transformationIndex`, `transformationIssues`, `transformationReferences`, `transformationSources`, `closureManifest` from content.mjs. `transformationFor` returns null outside pilots for UI; raw index retains all15. Pilot object has id,title,checkpoint,inputManifest,waitResume,invariants,validationQuestion,currentSummary,targetSummary,changes. Each change: id,title,current,target,boundary,type,fields,currentSources,targetSources,references; references include alias,canonicalRef,title,kind,observation,adaptation,limits,url(public only),sourceStatus (inherited; not fresh verified).

- [ ] Write failing behavioral tests: all94 change IDs resolve to144 occurrences; both M0.1 occurrences remain distinct; GAP6/MATCH7; imported metadata never changes business fixture; unknown scene null; EN pilots no Chinese prose; public reference projections exclude internal paths; source/proposal/observation separate; malformed source reference rejected by builder.
- [ ] Run RED before implementation. Build deterministic artifact from raw JSON, preserving complete source records/issues/closure manifest. Do not use a network or treat document receipt as application verification.
- [ ] Localise all13 pilot changes and their context/control/validation text into clear en-AU/en-US; same English acceptable. Chinese uses exact source prose except display title can use concise translation. Other13 scenes retain raw Chinese in offline content only, not masquerading as English UI.
- [ ] Map20 aliases against existing reference registry by exact official URL/title identity and preserve distinct internal design classification. Do not duplicate an existing registry source or fabricate URL. Supply alias mapping data; host reference adapter is Task2. Missing mapping stays explicit.
- [ ] Verify source counts and locator integrity, record source deck hash comparison separately (controller). Full Node suite once. Self-review and report RED/GREEN, exact files and limitations. No app/CSS/engine edits, no subagents/commit.

## Task 2: Pilot comparison + shared presentation hierarchy

**Files:** Modify `prototype/app.mjs`, `content.mjs`, `navigation.mjs`, `index.html`; create `prototype/transformation-ui.mjs`, `prototype/presentation.css`, focused tests. Scoped display edits allowed to `batch-a-ui.mjs`, `collaboration-ui.mjs`, `screening-ui.mjs`, `specialist-ui.mjs`, `specialist-graph.mjs`, reference UI and related CSS where audit proves prominent identifiers. Do not edit business engines.

**Interfaces:** Attach adapter through `content.mjs` to existing scenarios. `transformationHtml({scene,locale,comparison,compare,context})` renders selected pilot. Persist presentation-only compare/tab/selected-change state in navigation so return/reference restores it. Existing n/data/session remain authoritative.

- [ ] Read controller fresh screenshot audit before changing hierarchy. RED tests for pilot single/compare output, escaped sources, smaller metadata, no raw IDs as action headings, locale, same input context, presentation-only navigation.
- [ ] Existing GAP/MATCH modal gains one compact transformation section: single Current/Target by default, explicit Compare,13 expandable work rows split6/7. Action title leads; current/target activity, control boundary, four provenance lines and field details progressively disclosed. All selected content printed regardless open/viewport; no cloned business database.
- [ ] GAP/MATCH References select existing registry records via aliases and preserve node/tab/current context. Internal project references never become public file links or client task labels. New public reference observations retain supplied verification provenance, no fresh-source claim.
- [ ] Current entry button says View target product. Preserve existing real B/C work actions and Save/Discard/Stay. Do not change authored business outcomes or auto-load a fixture on comparison.
- [ ] Demote technical labels in Studio, all modal families, workspaces, references, graph control labels, static/print wrappers. Human-readable graph node names lead; ID secondary. Prefer semantic markup, not arbitrary DOM text rewriting or broad blanket opacity/font shrinking. Do not hide meaningful values/errors/status. Root supplies HTML inventory and graph asset handling.
- [ ] Keyboard/focus/reduced motion/mobile/print must remain usable. Main body ≥14px, auxiliary metadata ≥11px with readable contrast. Retain existing IDs in attributes/data/traceability. Full suite once and report. No subagents/commit.

## Task 3: Controller audit, existing diagrams and verification

**Files:** `00_governance/d3/`, `prototype/qa/d3/`, `07_output/2026-09-07-d3-pilot-review.md`; existing diagram binding overlay/specs only if needed. No business engines.

- [ ] Produce source-ambiguity/cross-document-conflict/display-field lists before shared edits. Confirm E/F pending and document why read-only D3 can proceed.
- [ ] Inventory every repository HTML by active mainline, media/static, standalone graph, QA/archive and experiment; inspect active families and distinguish static scan vs actual visual coverage. Never rewrite archived baseline/QA proofs.
- [ ] Capture and inspect fresh before/after evidence for Studio, GAP, MATCH, Product, D graph/reference and selected print/mobile. Record audited routes and any inaccessible family rather than claiming all files visually tested.
- [ ] Reuse existing Archify GAP/MATCH diagrams and stable node IDs; create reviewed D3 change/reference bindings to actual nodes. Any new/changed graph rendered through installed skill, not handcrafted flowchart.
- [ ] Run full Node tests, D3 content/return/locale/compare/draft/print/read-only checks plus preserved A/B/C/D host regressions with new receipts. Source counts are content checks, not bank verification. Record passed/failed/not-run literally.
- [ ] Task-scoped and final review with exact baseline diff. Stop at two-pilot human review; do not automatically expand13 or implement E/F. Deliver local previews, screenshots, actual evidence and unresolved questions.
