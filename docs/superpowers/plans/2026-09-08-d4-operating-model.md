# D4 Operating Model — Person T pilot

Goal: execute approved OM-T00–T06 only. The 2026-09-08 user approval and `/Users/christinaliu/Downloads/Clear_to_Trade_D4_Operating_Model_Studio_Codex_Brief_v1.0.md` are the implementation contract. Preserve earlier A–D business contracts and D3/D3A. No remote publication or commits.

## Global constraints

- Native HTML/CSS/ES modules, current in-place dirty checkout. No new framework, model, Agent runtime or integration. Source files are design input, not evidence of implementation or bank approval.
- Only SCN-MATCH / BR-07 / WF-05 pilot; 17 original D4 actions in six approved display groups. Default D4A-MATCH-04-01. All IDs/source positions/initial score status remain. Workflow is a container, Human gates cannot be scored away.
- Author views and playback are read-only; do not edit five business engines, fixtures, writers, grants or schema. Actual commands remain in existing C Screening and B collaboration. No case reset or implicit fixture load. Product return preserves latest saved data and locale.
- Studio subpage, not third main entry. Four views only; human-readable titles, secondary IDs, black/blue/white, no violet/mint. All three locales. Separate customer English export physically excludes internal/Chinese payloads, not CSS alone.
- Exact sources: Downloads D4 JSON SHA fdaa5d5c172645dd2f0409bbc929a016dd5d78ba0ea24d3b15f2fd79ba3852ad; D4 main MD; MATCH section lines3830–4465 of All Scenes MD; newest Brief; existing D3A/D3 registries, C runtime and A/B interfaces.
- Archify v2.17 diagrams, existing parents in `../experiments/methodology-operating-model-r2`. Use derived suffix, preserve originals. Root owns diagrams and final browser/PDF QA; implementer must not touch these paths.
- New author projection data under `prototype/operating-model/`; raw source under governance/source outside public export. Four status dimensions; no fake implemented/deployed claims.

## Task 1: Implement the bounded Operating Model pilot (OM-T01–T04 and unit-level T05)

Owner: one isolated implementation subagent. Allowed edits: `prototype/operating-model/` (except diagrams), `prototype/operating-model-state.mjs`, `prototype/operating-model-ui.mjs`, `prototype/operating-model.css`, `prototype/navigation.mjs`, `prototype/app.mjs`, `prototype/index.html`, minimal exact D3 node link hook in `prototype/transformation-ui.mjs`, new `prototype/tests/operating-model*.test.mjs`, generator/export scripts in `00_governance/operating-model/` (not baseline or QA), `00_sources/d4/`. No changes to business engines or other existing source.

1. Read entire newest Brief, inspect actual app/navigation/C commands and D3 mappings before code. Import canonical source exactly with mechanical copy; derive immutable local projection. Store raw Chinese/legacy collaboration patterns in author-only source; fully translate 17 actions, score rationales, controls and 12 skill families. Reuse edges for full229 from actual `skill_refs`; other scenes no expanded UI. Existing AG candidates/logical names remain deep metadata. Source-qualified crosswalk retains all exact current/target occurrences and issue refs. No new business IDs.
2. TDD: write and run focused failing behavior tests before each feature. Test missing/stale/unknown data and access denial. Projection must derive scores and reuse from source (not handcopy numeric truth); both supports and executes distinguishable. No generic unqualified source fallbacks.
3. Four-view UI with six groups/17 selectable actions, expanded selected group, exact three comparison cases, collapsed read-only seven scores + N/F reasons; no score editing. Rule + Skill + Human + conditional candidate labels. Fit selection, Previous/Next action. Inline sources with observation/transfer/limit and source-format ambiguity. Preserve D3 change classifications.
4. SK03/SK06 full contract/input/output/cited synthetic sample/boundaries/stops/human use/status. Related-first skills, explicit12family catalogue. Inspect usage stays context; Open scene explicit scope-limited notice with return. Canonical ID disclosure. Separate status dimensions, no runtime validation claimed. Existing `Agent-prepared` corrected at display according to primary executor and relation, canonical source unchanged.
5. Six pre-authored frames paused initially; F3/F4 auto pause. Ordinary Next/Play cannot cross to F5; explicit Preview later authored example required, labelled as authored outcome not user decision. Return pauses. Reduced-motion static. Timeline only modifies navigation, no imports of business mutation APIs in OM modules. If no reviewed author checkpoint actually exists, label unavailable and use existing authored unresolved/referral continuation without inventing approval; report source.
6. Main app hooks minimal: secondary global Studio entry, SCN-MATCH scene entry, exact D3 MATCH node→appropriate original D4 action. State holds only references/origin nav/camera/scroll, no case snapshot. Current-origin restored. Deep route `?studio=operating-model&scene=SCN-MATCH&view=...&action=...`, semantic hash/history, invalid old refs readable with safe scene fallback. Pausing on reference/panel/leave. Product token Back to operating model, scenario/journey choices, current session revision notice. Existing Save/Discard/Stay and failed save honored. Missing C PersonT target or D session => explicit unavailable, never generic legacy EntityA finding. Existing C actual Finding target allowed when present.
7. Root will generate canonical SVG paths under `prototype/diagrams/operating-model/`. Bind through manifest that root supplies at `prototype/operating-model/diagram-manifest.mjs` (root owns this file) exporting `operatingDiagram(view,locale)` returning `{id,parentId,src,alt,nodeRefs}`; expected view keys4. Semantic HTML node list always readable, selected/focus state exterior (no assumed Archify runtime callbacks).
8. Scenario Operating Model and Skill Brief full-content print in existing app; repeat human context/version/synthetic/legend; English and Chinese. Standalone English customer export generator must build physically whitelisted read-only content/refs/assets, not copy internal app bundle. Never run a remote host. Unknown role / RM / client cannot access internal author projection (UI denying alone does not claim auth); safe export is the only handoff-safe package. No private refs or Chinese in packaged JS/JSON/assets/HTML; document limitation of development server as author-only.
9. Run full Node suite once and report. Write detailed implementation report with RED/GREEN commands/results, file changes, source and checkpoint decisions, QA hooks/selectors, public export command/path, concerns. Do not run full browser/PDF QA (root owns). Do not spawn subagents. Do not commit.

## Task 2: Integrate verified Archify assets and validate host (OM-T00, T03, T05–T06)

Owner: root. Preserve baseline of prototype source files and hashes of five business engines before Task1 edits. Reuse current four parent Archify specs, create named MATCH derivatives with explicit parent refs and source/action anchors, neutral restrained palette. Use actual schema/renderer validator/export and inspect final SVG/PNG. Provide localized assets and manifest API agreed above; static list is authoritative if asset missing.

Create local Playwright browser checks for OM01–30 with actual evidence: full node selection and origin restoration, frame pause/gate and zero writes/network, scopes/roles, existing sameFinding round-trip with saved/unsaved/failedsave/currentrevision, history/deeplink/staleaction, allthree locales, wide/narrow/keyboard/reducedmotion, full scenario and skill print/public pack redaction. Run existing A/B/C/D and D3A regressions in proportion to hooks, no historic evidence overwrite. Render/inspect screenshots and PDFs, fix defects. Lab absent and actual model runs accurately NOT_RUN.

Independent task review after Task1 implementation using exact baseline delta; final strongest review after tested integration. Fix critical/important issues with focused tests. Publish local preview only; write `07_output/2026-09-08-d4-operating-model-review.md`, OM-T00 file/delta map, source crosswalk, actual checklist receipts and open bank questions. Stop at OM-T06 for Christina/Xiaoming/Coco; no other scenarios or Discussion expansion.
