# Batch E then F Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Follow task-scoped tests and reviews; no commits or remote operations in this dirty working copy.

**Goal:** Implement and verify QA remediation, scoped readiness, authorised demo confirmation and separate publication, then consolidate the approved D2 mainline.

**Architecture:** Extend the current synthetic Shared Case Spine and vanilla-module prototype. Reuse A–D physical objects and legacy adapters; add one E engine and projection with its own guarded commands. Studio, Product and read-only Lab consume that projection. Navigation and Archify remain presentation-only.

**Tech Stack:** Existing HTML/CSS/ES modules, Node tests, local Playwright/Chrome, installed Archify 2.17.

**Spec:** `/Users/christinaliu/Downloads/Clear_to_Trade_D2_Batch_E_QA_Readiness_Clearance_Final_v1.0.md` and `/Users/christinaliu/Downloads/Clear_to_Trade_D2_Batch_F_Consolidation_Final_v1.0.md`.

## Global Constraints

- Same `DEMO-CTT-001`: Entity A requests FX forward; Entity B is the reported Singapore parent; Person T works for B. Coordination, identity, signing and trading authority remain separate.
- User approved E implementation and verification before already approved F. This overrides E's historical stop-before-F sentence. Do not resume D6 or expand other Discussions.
- Preserve existing A–D, D3, D3A, D4, D5 and UX audit changes. Work in place; do not commit, reset, push, publish, connect bank systems or send messages.
- Final means demo design approved, not bank policy, legal applicability, authority, production integration or test completion.
- `NOT_READY`, `READY_FOR_AUTHORISED_CONFIRMATION`, `CLEARED_TO_TRADE` are distinct. Completion of QA, screening, specialist work or publication is never a trade.
- Unknown or absent scope, applicability, dependency, permission and stale inputs fail closed. Empty sets cannot manufacture readiness. Evidence and historical decisions remain immutable and traceable.
- Use existing `clearanceConditions`, `dependencies`, `holds`, `evidenceUseAssessments`, `readinessSnapshots` and source-qualified aliases. Do not introduce parallel truth stores or numeric-only DEP mapping.
- Only two top-level experiences. Normal operational titles; technical IDs secondary. Black/blue/white theme, no violet or mint. No completion percentage or teaching subtitle in Product.
- Story/Play/Next, diagram focus, language, return and printing never write business state. Latest locale and saved state survive return. Save/Discard/Stay on dirty exit; failed save stays.
- Lab is optional, reads the same revision, and never writes mainline. Static media fallback is mandatory; no asset fabrication.

### Task 1: E data, guarded events and state oracle

**Files:** Create `prototype/clearance-engine.mjs`, `prototype/tests/clearance-engine.test.mjs`, `04_operating_model/batch-e/build-contract.mjs` and generated JSON field/schema/alias/snapshot contracts there. Change `prototype/case-engine.mjs` only for an additive E-readiness adapter if necessary; preserve all legacy semantics for non-E. No app/UI/navigation edits in this task. Capture before-images of any existing changed file into this plan's workspace.

**Read first:** Entire E Final; `prototype/specialist-engine.mjs`, `prototype/screening-engine.mjs`, `04_operating_model/batch-d/downstream-e-interface.json`, D entry snapshots, relevant B ownership requirement/evidence objects. Inspect source IDs in all current manifests before assigning E/F IDs. The top-level CSV only lists SRC-001–008; later source namespaces already exist in subordinate manifests.

**Interfaces:** Produce `createBatchE(input)` (explicit additive authored entry, same case), `clearanceProjection(state,{role})`, `clearanceLenses(state,{role})`, `clearanceAction(state,command)` immutable success/error with `.code`. Projection includes availability, scope/version, three-layer prerequisites, exact readiness state, permitted action reasons, current input revisions, QA/remediation and safe publication summary. Commands use role, expected case/scope/input revisions, key, rationale and timestamp. Publish explicit API/action contract for Task 2.

- [ ] Reconcile E alias chain `REQ-B05 → EUA-05 → QA-04 → GAP-QA-01 → RT-01` with actual B ownership/control objects. Reuse semantic matches and retain raw source alias. Add missing E objects with stable case IDs, not competing B requirements.
- [ ] Write independent literal behaviour tests before implementation; RED is a semantic failure, not an import typo. Example contract:
```js
const entry=createBatchE(dEntry);
assert.equal(clearanceProjection(entry,{role:'ROLE-QA'}).readiness.readiness_state,'NOT_READY');
assert.equal(entry.scopes[0].booking_entity_id,dEntry.scopes[0].booking_entity_id);
```
- [ ] Implement approved E entry without closing any upstream unknown. Add QACheck, RemediationTask, ClearancePrerequisite, ReadinessSnapshot, ClearanceDecision, PublicationEvent logical fields from E §5. Store in existing equivalent collections where available. Keep schema candidate, metadata source-qualified, bank authority null.
- [ ] Implement explicit reviewable synthetic events for opening exact QA gap, remediation submission/completion, QA re-review and signoff. Remediation completion never signs QA off; QA can rereview only affected purpose/current evidence. Preserve unrelated Legal/Credit records.
- [ ] Implement closure events for conflicts resolution, full inventory/coverage, legal execution with current Credit and independently established signatory authority, and independent EDD/context applicability where required for the E final state. These must consume a named, inspectable E-approved synthetic package and literal oracle; do not infer resolution from C's unresolved Person T decision or D's entering page. Record new evidence/assessments/decisions preserving earlier C/B history. Missing package or changed inputs disables its action. No arbitrary booleans that mark conditions green. The exact E brief approves authored scenario events, not real bank decisions.
- [ ] Model current scope with booking unknown until an explicit sourced synthetic demo-context event; never silently fill bank fields to satisfy the legacy `candidate_ready` function. Demo context must remain synthetic and bank policy/authority null. Unknown product eligibility/DD/ownership/EDD must each be resolved through explicit scoped demo records before candidate readiness.
- [ ] Derive PREREQ-01 KYC/evidence/QA, PREREQ-02 complete applicable screening coverage, PREREQ-03 specialists from actual current objects, expected dependencies, holds, authority and versions. Missing expected dependency is unknown, not independence. Add readiness snapshot history without mutating earlier snapshots.
- [ ] Guard human confirmation with explicit `Authorised Clearance Reviewer` demo role/action/scope config, current ready snapshot fingerprint, rationale and authority reference. Reject unknown/unauthorised/stale/cross-case commands. Record ClearanceDecision + timestamp; publication separate command with simulated permitted targets. Failure retains decision; never executes trade. Scope/evidence changes invalidate current applicability but retain historical clearance.
- [ ] Write and run all E core RT cases 1–17 and state portions 18–20, including malformed/missing conditions, tampered authority, stale inputs, duplicate/reused command IDs and mutation isolation. UI portions stay NOT_RUN until Task 2. Use `node --test prototype/tests/clearance-engine.test.mjs`, then full `node --test --test-reporter=dot prototype/tests/*.test.mjs` once. Tests must demonstrate exact positive path as well as negative guards.
- [ ] Create generated candidate schema, field dictionary (type/nullability/owner/provenance/revision/UI mapping), dependency/alias delta, action contract, independently literal expected results and generated snapshots with event refs. Build script must use apply_patch for generated text files or preserve project's existing justified mechanical generation workflow.
- [ ] Self-review and write full report including changed paths, RED/GREEN commands/output, exact supported actions, named fixtures, any unresolved implementation versus business questions. Do not commit. Return concise status and report path.

### Task 2: E Studio, Clearance and remediation interaction

**Files:** Create `prototype/clearance-ui.mjs`, `prototype/clearance.css`, focused native and browser tests; modify `prototype/app.mjs`, `prototype/navigation.mjs`, `prototype/content.mjs`, `prototype/index.html` and existing scenario/reference records only as necessary. Use root-generated E Archify artifacts at `prototype/diagrams/batch-e/`. Snapshot changed files before edits.

**Interfaces:** Read Task 1 action contract/report and import its engine/projection; no duplicate readiness predicates. Expose operational `clearance` and `qa-remediation` steps, E story beats inside existing SCN-QA/SCN-READINESS/SCN-PUBLISH. Preserve current C/D/D5 entry and archive boundaries; no silent fixture load on navigation.

- [ ] Read E Final §§3–10, existing navigation/app/specialist UI and audit style before changing UI. Write failing interaction tests for E routes and state-safe navigation.
- [ ] Implement two patterns only: Clearance header (entity/product/booking/scope revision/readiness) + six condition groups with status/reason/owner/wait/next action; QA Remediation Detail with exact requirement/use/check/gap/task chain and activity. IDs in secondary disclosure; no instructional subtitle or percent. All user copy zh-CN/en-AU/en-US.
- [ ] Expose explicit E entry from current valid D session, or clearly reviewed archive/load path, never navigation-induced state changes. Only engine-approved actions enabled, with visible missing reason. Role controls belong to workshop shell; unknown bank authority remains validation question, not production access.
- [ ] Implement save/rationale, remediation, closure review, final authorisation and publication actions through Task 1 engine. Each action shows actual new records and remaining blockers; no fake success. Preserve rejected input. Redact restricted internal rationale before RM/client DOM/print.
- [ ] Implement E1–E6 Current/Target scene work contracts and meaningful next steps. Diagram selection/focus/play only changes presentation; use root's DG-E01–03 same-source static outputs and HTML host controls. Missing media remains static, no fake playback.
- [ ] Preserve navigation state: case/scope/scene/role/stage/trigger/comparison/locale/region/story/zoom/viewport/session. Prototype → Scenario, Journey and Browser Back restore meaningful origin without overwriting business state/latest language. Save/Discard/Stay and save-failure paths tested. Print complete selected E scope regardless viewport with safe projection and static diagrams. Lab read-only same snapshot and mainline works with Lab disabled.
- [ ] Run E browser RT18–20 and interaction suite, plus A–D/D5 regression. Use local Playwright import `audit/tooling/node_modules/playwright/index.mjs` and Chrome `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`; local URL port 8765. Collect English/Chinese, desktop/mobile and print screenshots. Actual passed/failed/not-run evidence, no fabricated coverage.
- [ ] Self-review, full report and diff paths; do not commit or touch F before E review gate passes.

### Task 3: F consolidation after E verification

**Files:** Update existing registries in `03_personas_journey`, `04_operating_model`, `05_product_requirements`, `00_governance`; add clearly derived consolidated exports under `00_governance/batch-f` and `04_operating_model/batch-f`. Update existing Studio secondary content/navigation minimally; no new top-level page. Tests in `prototype/tests/consolidation.test.mjs` and local browser tests. Root supplies DG-F01–03 Archify separately.

**Interfaces:** Consume verified A–E same-case contracts and E engine API; retain all older source-qualified IDs and statuses. F exports are derived indexes, never second runtime stores. User authorisation allows this task only after E RT/core return verification is complete.

- [ ] Read entire F Final and previous F reconciliation. Record implementation_duplicate/implementation_defect/source_conflict/design_assumption_conflict/business_validation_required separately. Preserve prior paused-preflight evidence as historical.
- [ ] Build one master Living Story with actual scene IDs from scope/entity through requirement/source/residual request, population/match, parallel specialist work, QA, readiness, confirmation and publication. Each has Trigger→Work→Decision→State change→Next scene, inputs/outputs/deps/source status and exact product/return links. Story never executes writes; explicit approved snapshots remain labelled and preserve session archive.
- [ ] Consolidate object/field/state/dependency mappings across eight domains. Reuse existing canonical stores, map legacy enum differences explicitly, preserve applicability/response/review/approval/execution/publication distinctions. DG-F02 affected/unaffected/unknown projection reflects dependency versions and holds; absent edge cannot prove independence.
- [ ] Integrate a Product Definition summary as secondary Studio view and existing diagrams/ref navigation. Use existing source registry, aliases and observations; deduplicate only same URL/version/context, preserve FHIR R4/R5. Keep why/adopt/adapt/avoid and scenario/node bindings. Inherit original verification status; actual link checks separately dated, never invent screenshot/media review.
- [ ] Populate existing capability_map and requirement_backlog equivalents using existing CAP/CTT-REQ IDs where allocated; if headers only, allocate once and register. Ten F capability families, explicit human boundaries, requirement journey/process/capability/source/assumption traceability and observable Given/When/Then acceptance. No priority scores or final MVP ranking.
- [ ] Consolidate Friday validation questions using existing Q IDs where semantic matches: Case Manager/Ops, final authority, population, EDD, Legal/Credit early start, client channels, RM visibility, hold scope, downstream systems, publication failure ownership, evidence reuse, case representativeness. No inferred client answers.
- [ ] Add master G1–G6 red-team register that points to actual automated cases/evidence, not a blanket pass. Run tests for empty/stale/unknown scopes, claims/uses, permissions, dependency impact and false clearance plus end-to-end cross-language return/print/readonly graphs/Lab. Record any unimplemented paths honestly. Existing audit residuals remain separately visible.
- [ ] Generate final reviewable incremental report: preview entry, file/schema/dependency deltas, diagrams/static/print, real test outcomes, references/media status, limitations and questions. Stop after F; no D6, push or publication.
