# Approved panel-first/r01 mainline integration

Spec: ../../../../experiments/d2-batch-a-media/MAINLINE_INTEGRATION_HANDOFF.md (absolute source /Users/christinaliu/Documents/ChatGPT/Australia Bank/experiments/d2-batch-a-media/MAINLINE_INTEGRATION_HANDOFF.md).
Only DMO-A01/02 into existing SCN-SCOPE/ENTITY. Reuse selected fixed media, no new case schema/store/business actions, no remote publish/push/commit.

## Task 1: Isolated read-only player component
Own ONLY prototype/batch-a-media.mjs, prototype/batch-a-media.css, prototype/tests/batch-a-media.test.mjs, .superpowers/sdd/2026-09-07-media-mainline/task-1-report.md.
Read approved source src/content.js, src/views.js, src/player.js, styles/media.css, locales and handoff. Main agent will package fixed source content/views, styles/media.css, locales, personas and diagrams under prototype/media/d2-batch-a/r01 with identical relative structure. Do not edit packaged/source files or main app.
Build reusable mountMedia(element,{scene,locale,comparison,snapshot,onChange}) async returning {pause(),destroy(),snapshot()}. Export pure initialMediaState(scene,saved) and reduceMedia(state,intent) for meaningful tests. scene allowlist SCN-SCOPE->DMO-A01, SCN-ENTITY->DMO-A02; locale zh-CN/en-AU/en-US; comparison current/target. snapshot shape {asset,revision:'r01',time:number,beat:number,holdPassed:boolean,playing:boolean}; normalize unknown input, never accept stored playing=true. No business store imports or postMessage listener. onChange only sends sanitized media state. No global test API needed; data attributes on media host expose time/beat/playing for browser tests.
Use a shadow root to isolate approved panel-first CSS from mainline. Reuse fixed source views/content as read-only narrative projection, not real case data. Reuse the exact selected portraits and existing approved purpose/evidence panels, plus cue descriptions, Current/Target organizing-role copy and Not ready. Outer lab nav must not be embedded. CSS may be a scoped bridge for host blue/white/glass, retaining approved r01 layout. At mobile collapse layout without clipping. Load assets through new URL relative to import.meta.url; no experimental origin/4182.
Initially paused; only explicit Play starts; Play/Pause/Previous/Next/Replay, no end loop. DMO-A02 must stop at9.5 seconds cue A02-C4, Play cannot bypass, Next advances13 seconds and unlocks later beats. Replay resets0 and remains paused (explicit Play required). Arrow keys scoped to focused component controls; i-help keyboard/focus/click/Escape, no host modal accidentally closed. Reduced motion disables motion/auto timeline or provides static stepped mode; honor OS change. Focus highlight is local and read-only.
Pause/destroy on host request, focus loss, document hidden. Clean event listeners/RAF/fetch stale results. Restore each scene time paused; preserve locale/comparison host inputs. No autonomous related scene navigation; host owns it.
Use exact local canonical SVG as img (no hand-drawn replacement). Local focus can be panel highlight / cue focus chips instead of editing canonical SVG. If images/diagrams/CSS unavailable, readable text fallback and all important unknown/purpose/state meanings remain, never blank. Main fallback outside component handles module/locales failing entirely.
TDD: test reducer stubs fail for initial autoplay, malformed/unknown intent, seek boundaries/hold and no mutation, then implement. Unit tests use real reducer. Provide main API note early. Do not spawn agents or commit.

## Task 2: Main host integration and versioned packaging
Controller packages selected runtime/content/assets/hash provenance; compares typed diagram semantics and records source/mainline alias mapping without overwrite. Extend existing batchScene slot, static story/print locale resources and app media lifecycle. Preserve real business engines and legacy data. Add direct URL scene query for mainline preview; scene query selects navigation only. Preserve modal scroll, camera, Current/Target, latest locale, per-scene beat/time, live and dirty workflows. Optional dynamic import must fail to complete static fallback.

## Task 3: Host QA and review
Fresh browser checks: selectedassetslocalorigin/no404; all controls/9.5hold/end; close/crossscene/product/blur/return pause; all3locales2comparisons; reducedmotion/i-helpkeyboard; 1440/1366/390/200percent reflow; business hash beforeafter malicious/repeated mediaevents; dirty save/discard/stay baseline; allassetsblocked and player-moduleblocked static fallback; print all5+6 storybeats notcurrentframe. Full existing85tests plusnewtests; no reuse of sample14/14. Capture screenshots and local printable static proof. Independent component and final integration review then bounded fixes.

## Task 4: Handoff
Update mainline media manifest only actualreceivedselected4assets; optionalDMOA03/VID remainnull. Separate source tests fromhostevidence. Report tests, limits, source hashes, previewURLs and unresolved businessquestions. No remote service.
