# Batch C Final bounded implementation

Spec: /Users/christinaliu/Downloads/Clear_to_Trade_D2_Batch_C_Screening_Review_and_EDD_Final_v1.0.md.
C-T00 map:00_governance/batch-c/file-delta-map.md. Execute only this batch; no full rewrite, commit, push, remote service, production policy or live integration. Preserve A/B IDs/states and frozen r01 media. Same case, Person T. Exclusion disabled pending separately reviewed evidence/oracle. Native ESM stack. Tests first for behavioral changes. All agent work uses apply_patch and no subagents.

## Task 1: C model, guarded actions and B reuse (C-T01–05)

Own prototype/screening-engine.mjs, screening-predicates.mjs, prototype/collaboration-engine.mjs, prototype/tests/screening-engine.test.mjs, prototype/tests/screening-collaboration.test.mjs,04_operating_model/batch-c/build-contract.mjs and its generated outputs. Do not edit app/UI/content/navigation/diagrams/references or existing fixtures/schema/tests. Read actual case/batchA/B modules plus Final§§1–11,16–20,23–24. C-T00 binds aliases.

Implement createBatchC(input) additive to current B case, no automatic upstream completion or reset. Require existing B context; preserve original arrays/objects and append C records only. C entry partial, no new identity evidence/finding before event; separate existing EntityA finding from C PersonT finding. Versioned population/membership, run/snapshots/provider/comparisons/coverage; logical fields follow Final§10 and field dictionary metadata. Export C_IDS and stable action API screeningAction(input,a) with role,expectedRevision,expectedScopeRevision,expected input versions,key,rationale. Error has stable code for locale. Export screeningProjection(input,{role}) allowlist for client/RM, plus shared screeningPredicates(input,{role,...context}) consumed by actions and graph binding. Send API note early.

Actions: prepare/request preliminary synthetic PersonT query (explicit inclusion/query/permission/hold config, not jurisdiction-derived), named completed/zero/partial/failed/pending outcomes with immutable run input/result (resultCount null for failure), latest run/historical late response guards; no comprehensive without confirmed current inventory and plan. Save review draft; request precise identity gap using B request/item/recipient/grant machinery; refer anytime permitted including urgent material concern with missing evidence, owner Unassigned unless configured; record unresolved scoped outcome; exclusion always blocked pending fixture review in shipped profile. Rationale, actor scope, revisions, idempotency fingerprint, evidence intake/use and holds must be checked at action time; no false empty coverage. Unknown hold fails closed for affected work, not blanket invented release. Keep histories; input mutations never silent.

B reuse: add optional requestId (default COLLAB_IDS.request) to collaborationAction and collaborationProjection. Enforce request membership for item/recipient/grant/session, and filter every request-scoped loop/projection. Add C targeted request using existing structures, separate contact/disclosure/access review and narrowly scoped new grant, original B items/grants untouched. Dispatch allowed_purposes checked against selected items; explicit C identity channel policy distinct from B. Reuse submission/intake/link path. Identity artifact alias EV-ID-C01 assigned only after submission (map physical evidence ID), not EV-A05. Assess identity use separately; insufficient/unknown unresolved path works, no canned sufficiency. C purpose config must not enable B coordination assessment for identity. Client-safe text never includes provider details; RM has safe progress only, not personal response text. No second messaging system.

Three branch types use WorkItem refs and fixed objects: information_gap/referral/conditional_edd. Multiple linked tasks permitted, repeated gap increments attempt/review history, no new scene per loop. resume recomputes versions/permission/intake/use/holds. EDD assessment independent required/not_required/unknown; defaultunknown; only explicit reviewed demo reason/permission can write scoped applicability; not auto-derived from match or parent. Required branch limited pack/issues/condition approval contract, no second EDD app, noSoF/SoW mandatory invention. Three read-only lenses same revision plus disabled single shadow-inject interface; mainline never requires Lab.

TDD literal expected outcomes from Final, not helper-generated expectations. Cover applicable RT-C01–26,34–48,65–68; unresolved/referral paths actual writes; unshipped sufficient exclusion positive cases explicitly not-run/disabled. Add B cross-request leakage/isolation regressions. Run focused tests each change, entire existing100 suite once at end. build-contract emits entry/event snapshots, field dictionary with all metadata, additive candidate schema/content validation, literal oracle artifact and actions. No claim production merged schema. Report TDD RED/GREEN, commands/results, API, changed files, limitations at task-1-report.md.

## Task 2: Three decision graph families (C-T06)

Controller owns prototype/diagrams/batch-c and diagram semantic model/validation tests. Follow Archify2.17 skill; P/M/E01–12 and all Final§20 edges remain in semantic model, predicate IDs shared with Task1. Overview may group nodes with explicit mapping; focus includes necessary branches/unknown/hold, static table preserves all edges. Generate actual zh/enAU/enUS and Current/Target views without overwriting A/B. Canonical SVG/HTML/spec/binding/delivery receipts; inspect real images and bounded viewer checks. Static host controls allowed; never undocumented runtime API.

## Task 3: Reference registry and component (C-T07)

Controller owns prototype/references.mjs,reference-ui.mjs,reference CSS/tests and registry build artifacts. Read actual SRC017/018 original files, import all36 aliases and V metadata plusCfive, observations/adopt/adapt/avoid/status/version. FHIRR4/R5 separate, sameEDDURL source dedupe retains observations, WolfsbergPDF distinct fromlanding. No new web research required for inherited source registry; no new verified date. Restricted project source download/body excluded; only approved-safe summaries. Same central scene References tab/nodeR/globalfilter; publicofficiallinks targetblank noreferrer no sensitive params, no external scripts. Projection/print independent of expandedDOM. Reference UI is pure navigation output.

## Task 4: Mainline/UI integration (C-T04,08)

Own prototype/screening-ui.mjs,screening.css,app.mjs,content.mjs,navigation.mjs,index.html,collaboration-ui.mjs and focusedUI/navigation tests. Consume Task1 API, Task2 diagrams/bindings, Task3 reference renderer. Two entrances, all15 IDs, samecase. Real Population/Review/Evidence/EDD/Activity in Product, actual saves/requests/referrals, explicit disabled exclusion reason. Continue currentB case; optional explicit archive/useBpartial snapshot only, never URL-trigger reset. TargetProduct→CurrentScene return latestlocale/data/draft; branchobject andviewscroll preserved; Reference modal notnested, pause playback andbackorigin. Story is authored stepped/static projection noautoapprovals orfakevideoPlay. C screenshot/print role projections contain no restricted client/RM fields. Add selectedCscene print allstaticnodes/refs andProductprojectionprint, no viewport-only. Keep all existing media integration intact. TDD real rendering/nav, report scope and evidence. Reuse existing B collaboration UI with selected requestId: C identity copy cannot remain authority-statement copy; generic record_assessment must not claim coordination sufficiency for C. Preserve B labels/actions when default request selected.

## Task 5: Integrated QA and handoff (C-T09)

Controller runs all current unit tests, C field/schema/content validation, fresh Chromium flows and A/B/media regression. Record allRT-C01–68 individually with steps/expected/actual/tool/evidence/severity/businessquestionowner; not-run distinct from passed. Screenshots, precise saved-statebefore/after, locale/currentreturn/dirtyfailure/referenceanchors/drag/zoom/reducedmotion/staticprint/mediafailure/Lababsent. No realauth/scanning/regulatory certification claims. Task-scoped reviews and final independent review; bounded fixes and re-review. Output07_output/2026-09-07-d2-batch-c-review.md plus localpreview. Preserve branch and taskaudit artifacts; no remote publication.
