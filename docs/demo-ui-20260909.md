# Demo Portal UI alignment · 2026-09-09

Branch: `codex/xiaoming-main-flow-20260909`.

User scope: align the entire Demo with the two supplied portal references, retain the existing UX, and use the story's personas. Scenario Template remains outside this change.

## Delivered

All six pages in `08_inspire/app/` load `portal-theme.css`: Home, Case, Intake, Documents, EDD and Request details. The shared treatment uses a navy header, white workspace, blue links and selected stages, yellow primary actions, serif headings and restrained blue/grey panels. Home adds a market photograph and an overlapping Aria briefing. The briefing retains both expandable tasks and both task-owner tabs.

The photograph is copied unchanged from the existing project asset `scenario-template-versions/version-2/app/assets/market-hero.png`. The source directory is not modified. The Demo's copy is self-contained under `08_inspire/app/assets/`.

Person T replaces Annette in the header, greeting, contact/EDD fields and task labels. The contact role is Client contributor, with a synthetic `example.test` email. The EDD capacity starts at “To be confirmed”; the original Director, Authorised signatory and UBO options remain available. Existing team display names now match Alex Morgan (RM), Morgan Lee (KYC Operations), Sam Ellis (EDD review), Casey Reed (Legal) and Robin Hale (QA).

## Behavior preservation and verification

- Compared all existing control IDs, input types, required/name attributes, `data-*` hooks and inline handlers against the pre-change version. All existing contracts match: Case 12, Home 28, EDD 34, Documents 17 and Intake 82 tracked elements. Request details adds presentation/navigation wrappers; its record lookup script is unchanged.
- All inline scripts are unchanged. `app.js` and `daily.js` change persona text only; `home.js` and `intro.js` are unchanged. All four scripts pass `node --check`.
- Browser checks: Home task expand/collapse, prepared-response dialog open/close, client/bank task tabs, All requests / Browse 8 stages, Intake stage selection, Needs you filter, Harbourview search, case-specific next steps and request detail navigation.
- Intake checks: all six steps, Back, add/remove an owner and story contact defaults. Existing submission behavior is retained without creating a new submitted case during testing.
- Documents: exercised the local upload simulation with a generated blank PDF (`ctt-ui-test.pdf`); it displayed the existing received/validation state. No real client document was used.
- EDD: checked capacity default and Save draft feedback. This verifies the existing feedback interaction, not a new persistence capability.
- Aria: opened/closed the drawer and used a suggested question. Help: replayed the introduction, advanced and skipped it.
- Visual checks at the default 922 px viewport, 1440 px desktop and 390 px phone width. Phone Home, Intake, Documents, EDD and Request details had a document width of 390 px. The request table retains its own horizontal scrolling so all existing columns stay available.
- Fixed a phone-only Home navigation offset discovered during verification. Restored the browser's default viewport and returned to Home after checking. Browser error log was empty at the final check; `git diff --check` passed.

This update keeps the existing sample cases and simulation behavior. It does not connect the Demo to Scenario Template, change the map, or introduce bank integration.
