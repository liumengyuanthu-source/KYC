import fs from 'node:fs';import {createHash} from 'node:crypto';
const raw=JSON.parse(fs.readFileSync('00_sources/d3/Clear_to_Trade_D3_Transformation_Traceability_v0.2.json','utf8'));
const hash=p=>createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const deckHash=hash('00_sources/raw/Sanitised Process Map.pptx');
const source=raw.reconciliation_issues.filter(x=>['PPT-AMBIGUITY','SOURCE-ID','SOURCE-ALLOCATION','AUTHORITY','FLOW-SEMANTICS','PROVENANCE'].includes(x.type));
const cross=raw.reconciliation_issues.filter(x=>!source.includes(x));
const issue=x=>`- **${x.issue_id} · ${x.title}** — ${x.observation}\n  建议（未作业务迁移）：${x.proposed_handling} 需确认：${x.review_owner}。`;
const fields=[...new Set(raw.scenarios.flatMap(s=>s.changes.flatMap(c=>c.field_deltas)))];
fs.writeFileSync('00_governance/d3/reconciliation.md',`# D3 source / conflict / presentation reconciliation\n\nStatus: content-and-presentation pilot only; no E/F implementation or D2-complete claim.\n\n## Source identity\n\nSupplied pack refers to Sanitised Process Map(1).pptx. The repository file has another filename, but SHA-256 matches exactly: ${deckHash}. This proves byte identity; D3 source visual-inspection statements remain supplied provenance, not a new PPT visual audit.\n\n## 1. Source ambiguities\n\n${source.map(issue).join('\n\n')}\n\n## 2. Cross-document conflicts\n\n${cross.map(issue).join('\n\n')}\n\nAll15 proposals remain open review items. Keep current B/C applicability and D state guards. Missing assessment is not invalid evidence; Credit approval is not fulfilment. E/F are not present as complete implementations. Closure manifest is a design checklist only, never an automatic completing event.\n\n## 3. Added presentation fields\n\n${raw.transformation_record_contract.map(x=>`- ${x.field} (${x.type}): ${x.meaning}`).join('\n')}\n\nThe ${fields.length} unique field paths in the94 change records are candidate design metadata, not newly implemented business fields. UI imports15 existing scenario mappings but initially exposes only GAP and MATCH details (D3 §9.5). Source IDs remain exact in low-emphasis detail; no schema keys or primary objects renamed.\n\n## Verification boundary\n\nRaw pack has15 scenes,94 changes,144 source occurrences,20 reference aliases and15 reconciliation issues. The supplied receipt explicitly says application_tests NOT_RUN and diagrams NONE. Fresh importer/host/visual results will be recorded separately.\n`);
fs.writeFileSync('00_governance/d3/source-check.json',JSON.stringify({sourcePack:'Clear_to_Trade_D3_Detailed_Transformation_Pack_v0.2.zip',expectedDeckSha256:raw.metadata.source_deck_sha256,actualDeckSha256:deckHash,match:deckHash===raw.metadata.source_deck_sha256,comparison:'SHA-256 bytes only; not a new visual PPT audit',sourceStatus:raw.metadata.status},null,2)+'\n');
console.log({deckMatch:deckHash===raw.metadata.source_deck_sha256,sourceIssues:source.length,crossDocumentIssues:cross.length,candidateFieldPaths:fields.length});
