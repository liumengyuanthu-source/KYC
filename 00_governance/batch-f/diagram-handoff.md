# F diagram handoff — qualified static delivery

Source: SRC-021, approved synthetic demo design, not validated bank policy. Archify 2.17. Manifest: `prototype/diagrams/batch-f/manifest.json`.

## Delivered and unresolved

| Diagram | Deterministic checks | Static host / print | Canonical viewer |
|---|---|---|---|
| DG-F01 shared case spine, English / Chinese | Both 9/9, zero errors or warnings | Both exported; captures inspected | Both FAILED vertical viewport containment |
| DG-F02 dependency / hold impact, English / Chinese | Both FAILED routing validation | Twelve-node semantic fallback, no graphical pass claimed | NOT_RUN: no delivered HTML |
| DG-F03 connected experiences, English / Chinese | Both 9/9, zero errors or warnings | Both exported; captures inspected | Both FAILED vertical viewport containment |

All four canonical viewer checks pass their readability and viewer-chrome checks, but overflow vertically in the six tested viewport/theme combinations. Do not advertise these standalone HTML viewers as accepted previews. Mainline consumes passive neutral SVG plus semantic text, and must verify its own layout and print output.

The four static PNG captures were actually inspected: no observed clipping or text overlap. Canonical SVG geometry and labels are preserved in neutral black/blue/white host and print derivatives. This is a limited perceptual inspection, not a viewer containment pass or workshop acceptance.

## Bounded repair record

DG-F02 initially had main-path and routing diagnostics. Correcting the main path preserved the logical edges. A bottom-channel route was rejected; the subsequent explicit case route hit a lane header. Shortening the lane label and routing the dependency-to-permitted-work edge then left a crossing with the unknown-scope path. The last two results did not improve; Archify's bounded repair rule stopped further geometry edits. Both final candidate specs and their failing validate receipts are retained.

This limitation leaves the requested new graphical dependency overview unaccepted. The twelve semantic nodes and fail-closed dependency explanation remain available. Do not label all F graphical acceptance, all D2, or Workshop-ready as complete.

## Host contract

- Use manifest `locales` SVG paths; `en-US` aliases the English artifact. DG-F02 locale paths deliberately remain null.
- Keep every fallback step accessible, including after an image-load failure; selection / focus changes presentation only.
- No diagram, Play / Next, image or media event may write business state.
- Logical object links are not a serial happy-path workflow or a production architecture. Reciprocal object references belong in the schema / field crosswalk, not inferred from arrow direction.
- Studio and Product remain the only top-level experiences. Optional Lab is a read-only projection. Print is not another state store.
- Host and print checks belong to Task 3 receipts; canonical delivery, automated viewer checks and static perceptual inspection are separate evidence layers.

## Reproduction / evidence

`deliver.mjs` can validate individual candidates or deliver the four accepted typed specs. Do not rerun DG-F02 as a new repair without explicitly revisiting the bounded limit. Per-artifact `*.validate.json`, `*.deliver.json`, `*.visual-check.json` and `export-receipts.json` are authoritative; aggregate validation files may reflect only the last selected subset.

Neutral exports use `audit/tests/f-diagram-export.mjs` and shared `audit/tests/diagram-export.mjs`. The E wrapper was mechanically extracted to reuse the same helper; existing E artifacts were not regenerated by that refactor.
