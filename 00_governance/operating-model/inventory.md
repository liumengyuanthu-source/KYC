# OM-T00 — actual file / delta map

As inspected 2026-09-08. Scope is the approved Person T Operating Model pilot, not implementation of the entire D4 analysis.

| Existing source / component | Role | Increment |
|---|---|---|
| `/Users/christinaliu/Downloads/Clear_to_Trade_D4_Operating_Model_Studio_Codex_Brief_v1.0.md` | approved page contract | OM-T00–T06 only |
| Downloads `Clear_to_Trade_D4_Action_Assessments_v0.1.json` |229actions/94works/12skills/12workflows | immutable projection; verified SHA matches Brief |
| Downloads D4 AI Operating Model MD / All Scenes Analysis MD | methodology and action reasons | MATCH section only fully elaborated |
| `prototype/transformations/`, `prototype/reconstruction/` | D3 source and branch mappings | retain exact occurrences, classifications and open source questions |
| `prototype/app.mjs`, `navigation.mjs`, `index.html` | native host and navigation | bounded page, entry/return/history/print hooks |
| `prototype/screening-ui.mjs`, `screening-engine.mjs` | real local C Person T task | read-only target resolution; existing controlled commands unchanged |
| `prototype/collaboration-engine.mjs` | B request / response | no new writer or scope grant |
| `../experiments/methodology-operating-model-r2/source/D4_*.archify.json` | four original parent diagrams | preserve parents; MATCH derivatives with static fallback |
| `prototype/tests/`, `prototype/qa/d3a/` | existing Node/Playwright harnesses | new operating-model evidence; retain historic receipts |

Business engines to hash and preserve: case-engine, batch-a-engine, collaboration-engine, screening-engine, specialist-engine. No production integration. D2 E runtime is absent and F is consolidation/preflight, not evidence of all D2 implementation.

## Resolved boundaries and checks

- Current C Person T review exists, but a fresh A session does not contain this finding. Entry must report target unavailable until user explicitly uses the existing C session workflow; it must not substitute the legacy Entity A finding or load a fixture.
- Disposition is currently disabled pending a reviewed fixture and permission config. Playback cannot enable it. A later authored outcome is not a current user approval.
- Inherited action scores are preliminary, not measured performance or authority. `Agent-prepared` is a legacy author field; display from executor and skill relation.
- Existing dirty/untracked prototype is user work. In-place baseline snapshots, no new worktree, reset, commit, push or external publication.
- Local preview: `http://127.0.0.1:8765/prototype/`. Runtime: native Node tests; local Chrome via cached Playwright. Actual command/results will be recorded, not inferred from this inventory.
