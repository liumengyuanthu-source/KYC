import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {resolve,join} from 'node:path';
import {fileURLToPath,pathToFileURL} from 'node:url';
import {operatingProjection} from '../../prototype/operating-model/model.mjs';
import {actionTitle,recommendation,reason,controlText,stopText,resumeText,humanRole,skillContent,referenceTranslations,groupNames,groupNotes,frameContent} from '../../prototype/operating-model/content.mjs';
import {operatingDiagram} from '../../prototype/operating-model/diagram-manifest.mjs';
const root=new URL('../../',import.meta.url),publicRefs=new Set(['REG-DFAT-LIST','IND-WOLFSBERG','IB-ANZ-SECURE','XB-FHIR-R5']);
const escape=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paragraph=(label,value)=>`<h4>${escape(label)}</h4><p>${escape(value)}</p>`;
const ul=items=>`<ul>${items.map(x=>`<li>${escape(x)}</li>`).join('')}</ul>`;
export function customerContent(){
 const model=operatingProjection({role:'ROLE-DEMO-AUTHOR'}),l='en-AU';
 return {title:'Operating Model — Person T Screening Review',client:'Confidential Australian Banking Client',version:'D4 v0.1 · Person T pilot',status:'Design proposal · synthetic authored example · real implementation and bank approval unverified',legend:'Workflow contains work · Rule checks and records · Skill prepares · Human decides · Agent is a conditional candidate',scene:'SCN-MATCH',branch:'BR-07',workflow:'WF-05',
 groups:model.groups.map((g,i)=>({id:g.id,title:groupNames[i][0],note:groupNotes[i][0],action_refs:g.actions})),
 actions:model.actions.map(a=>({id:a.action_id,title:actionTitle(a,l),change:a.transformation_type,recommendation:recommendation(a,l),reason:reason(a,l),inputs:a.reads,outputs:a.outputs,skills:a.skill_refs,relation:a.skill_relation==='supports_human_action'?'Supports the human action':'Used by execution',human:humanRole(a,l),preconditions:a.preconditions.map(x=>controlText(x,l)),forbidden:a.forbidden_effects.map(x=>controlText(x,l)),stop:stopText(l),resume:resumeText(l),references:a.benchmark_refs.filter(id=>publicRefs.has(id))})),
 skills:model.skills.map(s=>({...skillContent(s,l),contract_status:'Defined design proposal',sample_status:['SK-03','SK-06'].includes(s.id)?'Authored synthetic sample':'Not authored in this pilot',prototype_binding_status:'Read-only interface binding',implementation_evidence_status:'Unverified; no model or runtime evaluation',uses:model.reuse.filter(x=>x.skill_ref===s.id).map(x=>({action_ref:x.action_ref,scene_ref:x.scene_ref,relation:x.relation,inputs:x.reads,outputs:x.outputs}))})),
 frames:frameContent.map((f,i)=>({id:`OM-F${i+1}`,title:f[0],text:f[2],action_ref:f[4],status:i>=4?'Authored outcome — not a user decision':i===2||i===3?'Pause boundary':'Authored frame'})),
 references:model.references.filter(r=>publicRefs.has(r.id)).map(r=>({id:r.id,title:r.title,url:r.url,locator:r.source_locator,observation:referenceTranslations[r.id][0],transfer:referenceTranslations[r.id][1],limit:referenceTranslations[r.id][2],status:r.verification_status}))};
}
function sample(id){return id==='SK-03'?`<h3>Authored sample · synthetic</h3><p>Input: Person T and record C01, original query and subject version. Subject birth date is unknown; provider years are 1970 / 1971 at year precision.</p><table><thead><tr><th>Attribute</th><th>Subject</th><th>Record C01</th><th>Candidate output</th></tr></thead><tbody><tr><td>Name</td><td>Person T</td><td>Person T</td><td>similar — no identity verdict</td></tr><tr><td>Birth date</td><td>Unknown</td><td>1970 / 1971, year precision</td><td>inconclusive — missing is not different</td></tr><tr><td>Place of birth / nationality</td><td>Unknown</td><td>Unknown</td><td>inconclusive</td></tr></tbody></table><p>Contract vocabulary: similar / different / inconclusive / not_comparable. A single differing field cannot automatically exclude identity. Source basis: original authored C-result comparison; D4A-MATCH-02-02. Viewing this sample creates no live facts.</p>`:id==='SK-06'?`<h3>Authored sample · synthetic</h3><p>Input: the same Person T finding and C01 comparison, current evidence-use status and allowed-action set. Subject identity remains insufficient.</p><p>Draft output: “Can this finding be distinguished? The same synthetic name is known; subject birth date and other identity context are unknown. Review original sources, request scoped information or keep unresolved / refer where authorised.”</p><p>Output is a cited preparation illustration from the original C-result comparison and D4A-MATCH-04-01 contract. It does not recommend exclusion, approve a person or record a decision.</p>`:'';}
function compactPublic(content,image){
 const context=`${content.client} · ${content.scene} / ${content.workflow} · To-be · ${content.version} · Synthetic authored example`;
 const common=content.actions[0].forbidden.filter(x=>content.actions.every(a=>a.forbidden.includes(x))),row=(a)=>`<tr><td>${a.action_ref}<br>${a.scene_ref}</td><td>${a.relation==='supports'?'Supports human':'Used by execution'}</td><td>${escape(a.inputs.join('; '))}</td><td>${escape(a.outputs.join('; '))}</td></tr>`;
 const margin=JSON.stringify(context+'\n'+content.legend).replace(/\\n/g,'\\A ');
 return `<!doctype html><html lang="en-AU"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(content.title)}</title><link rel="stylesheet" href="style.css"><style>@media print{@page{margin:23mm 12mm 12mm;@top-left{content:${margin};white-space:pre-wrap;font:9px Arial,sans-serif;color:#183149;vertical-align:middle}}}</style></head><body><header><p class="eyebrow">${content.client}</p><h1>${content.title}</h1><p>${content.status}</p><nav><a href="#overview">Operating Overview</a><a href="#execution-choice">Execution Choice</a><a href="#skills">Skills & Reuse</a><a href="#collaboration">Human Collaboration</a></nav></header><main><section id="overview"><h2>Operating Overview</h2><p>Six reading groups do not define a mandatory runtime sequence. Evidence gathering is conditional; independent approval follows the existing configuration.</p>${image('overview')}<table><tbody>${content.groups.map(g=>`<tr><th>${g.title}</th><td>${g.note}<br>${g.action_refs.join(' · ')}</td></tr>`).join('')}</tbody></table></section><section id="execution-choice"><h2>Execution Choice</h2><p>First establish permission, scope and current input. Human judgement and formal authority remain hard gates. Compare rules, bounded Skills, fixed workflows and conditional planning candidates.</p>${image('execution-choice')}<div class="print-two-columns">${content.actions.map(a=>`<article id="${a.id}"><small>${a.id} · ${a.change}</small><h3>${escape(a.title)}</h3><p><strong>${a.recommendation}</strong></p><p>${a.reason}</p>${paragraph('Inputs',a.inputs.join('; '))}${paragraph('Outputs',a.outputs.join('; '))}${paragraph(a.relation,a.skills.join(' / '))}${paragraph('Human role hypothesis',a.human)}${a.forbidden.some(x=>!common.includes(x))?paragraph('Additional forbidden effects',a.forbidden.filter(x=>!common.includes(x)).join('; ')):''}<small>Public references: ${a.references.join(' / ')||'Design proposal; no direct public example claimed.'}</small></article>`).join('')}</div><h3>Controls and human handoff</h3><p>Shared by all actions; outputs and additional restrictions are action-specific above. All writes require existing business commands. This static package performs none.</p><h4>Preconditions</h4>${ul(content.actions[0].preconditions)}${paragraph('Stop',content.actions[0].stop)}${paragraph('Resume',content.actions[0].resume)}<h4>Shared forbidden effects</h4>${ul(common)}</section><section id="skills"><h2>Skills & Reuse</h2>${image('skills')}<p>Every use independently requires current subject / purpose scope, permissions and versions. Shared families do not share prompts, access grants or professional standards. The authorised human consumer retains identity, materiality, sufficiency, authority and clearance decisions.</p>${content.skills.map(s=>`<article id="${s.id}"><h3>${s.id} · ${s.name}</h3><div class="print-two-columns">${paragraph('Purpose / kind',s.purpose+' · '+s.kind)}${paragraph('Input',s.input)}${paragraph('Output',s.output)}${paragraph('Boundaries',s.boundaries)}${paragraph('Stops',s.stops)}</div>${sample(s.id)}<p class="eyebrow">Contract: ${s.contract_status} · Sample: ${s.sample_status} · Interface: ${s.prototype_binding_status} · Implementation: ${s.implementation_evidence_status}</p><h4>Mapped usage by scene and action</h4><table class="reuse-table"><thead><tr><th>Action / scene</th><th>Relation</th><th>Inputs</th><th>Outputs</th></tr></thead><tbody>${s.uses.map(row).join('')}</tbody></table></article>`).join('')}</section><section id="collaboration"><h2>Human Collaboration</h2><p>Static authored story. A reviewed identity-exclusion checkpoint is unavailable. The later unresolved example is an authored outcome, not a decision made by the reader.</p>${image('collaboration')}<div class="print-two-columns">${content.frames.map(f=>`<article><small>${f.id} · ${f.status}</small><h3>${f.title}</h3><p>${f.text}</p><small>${f.action_ref}</small></article>`).join('')}</div></section><section><h2>Public sources and limits</h2><p>Inherited verification retained; no new source review or bank approval is claimed.</p>${content.references.map(r=>`<article id="${r.id}"><h3>${r.title}</h3><small>${r.id} · ${r.locator} · ${r.status}</small>${paragraph('Source observation',r.observation)}${paragraph('Transfer',r.transfer)}${paragraph('Cannot establish',r.limit)}<a href="${r.url}" target="_blank" rel="noopener noreferrer" referrerpolicy="no-referrer">Open original public source</a></article>`).join('')}</section></main><footer>${context}<p>${content.legend}</p><p>No operational case session, author assessments, private source files or business commands are packaged. Browser Print includes the complete content.</p></footer></body></html>`;
}
export async function exportCustomer(destination){
 const output=resolve(destination),content=customerContent(),artifacts=[];await mkdir(output,{recursive:true});
 const diagrams=[];for(const view of ['overview','execution-choice','skills','collaboration']){const d=operatingDiagram(view,'en-AU');const svg=await readFile(new URL('prototype/'+d.src.replace(/^\.\//,''),root),'utf8');const name=`${view}.svg`;artifacts.push([name,svg]);diagrams.push({view,name,alt:d.alt});}
 const image=view=>{const d=diagrams.find(x=>x.view===view);return `<img src="${d.name}" alt="${escape(d.alt)}">`;};
 const css=`:root{font-family:Arial,sans-serif;color:#183149;background:#f5f8fc;font-size:17px;line-height:1.6}body{margin:0}header,main,footer{max-width:1120px;margin:auto;padding:36px}h1{font-size:42px;line-height:1.2}h2{font-size:30px;color:#005b9e}h3{font-size:21px}h4{margin-bottom:0}h4+p{margin-top:4px}a{color:#005b9e}nav{display:flex;gap:24px;flex-wrap:wrap}section{margin:36px 0}article{background:white;border:1px solid #c4d7e6;padding:24px;margin:20px 0;border-radius:8px;overflow-wrap:anywhere}img{display:block;width:100%;max-height:550px;object-fit:contain;background:white}summary{font-weight:bold;cursor:pointer}table{border-collapse:collapse;width:100%}th,td{border-bottom:1px solid #bccfdd;padding:10px;text-align:left}.groups{display:grid;grid-template-columns:1fr 1fr;gap:20px}.eyebrow{font-size:12px;color:#4d667c}.recommendation{font-size:21px;color:#005b9e}.print-context{display:none}@media(max-width:720px){header,main,footer{padding:20px}.groups{grid-template-columns:1fr}h1{font-size:32px}}@media print{@page{size:A4 landscape;margin:24mm 12mm 12mm}:root{font-size:11px;background:white}.print-context{display:block;position:fixed;top:-19mm;font-size:9px;border-bottom:1px solid #abc}header,main,footer{padding:0;max-width:none}nav{display:none}section{break-before:page}article{padding:12px;break-inside:auto}h2,h3,h4{break-after:avoid}tr{break-inside:avoid}details>*{display:block!important}summary{display:block}img{max-height:125mm}a{text-decoration:none;color:#183149}}`;
 const printableHtml=compactPublic(content,image);
 const compactCss=css+'\n.reuse-table{table-layout:fixed;width:100%}.reuse-table th:first-child{width:24%}.reuse-table th:nth-child(2){width:14%}td{overflow-wrap:anywhere}@media print{:root{font-size:11px;line-height:1.45}article{padding:10px 0;margin:12px 0;border:none}article p{margin:5px 0}h1{font-size:27px}h2{font-size:20px;margin:12px 0}h3{font-size:15px;margin:8px 0}h4{font-size:11px;margin:5px 0}img{max-height:85mm;margin:8px 0}.print-two-columns{columns:2;column-gap:24px}.print-two-columns article{break-inside:avoid}.print-two-columns>p{break-inside:avoid}table{font-size:10px;line-height:1.4}.reuse-table td,.reuse-table th{padding:5px}section{margin:16px 0;break-before:auto}section>article{break-inside:auto}thead{display:table-header-group}.eyebrow{font-size:9px}.print-context{display:none}}';
 artifacts.push(['index.html',printableHtml],['style.css',compactCss],['content.json',JSON.stringify(content,null,2)]);
 for(const [name,text] of artifacts)if(/\p{Script=Han}|IC-PHKL|PPT-S\d|D3-ISS|a9d5e073|score_values|score_rationale|author-source|sessionStorage|screeningAction/u.test(text))throw Error(`Customer projection rejected: ${name}`);
 for(const [name,text] of artifacts)await writeFile(join(output,name),text);
 return {output,files:artifacts.map(([name])=>name)};
}
if(process.argv[1]&&import.meta.url===pathToFileURL(resolve(process.argv[1])).href)console.log(await exportCustomer(process.argv[2]||fileURLToPath(new URL('07_output/operating-model-customer-en/',root))));
